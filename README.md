# AI-Log-Analyser
AI-powered log analysis dashboard that reconstructs distributed request lifecycles from mixed logs using Trace IDs, performs root cause analysis, and visualizes system health through an interactive Flask dashboard.
